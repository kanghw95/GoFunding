<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:websocket="http://www.springframework.org/schema/websocket"
	xsi:schemaLocation="http://www.springframework.org/schema/websocket http://www.springframework.org/schema/websocket/spring-websocket-4.3.xsd
		http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.3.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />
	<resources location="/smarteditor/" mapping="/smarteditor/**"/>
	<resources location="/sb-admin-2/" mapping="/sb-admin-2/**"/>

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- NaverLoginBO Class에 대한 Bean설정 추가 -->
	<beans:bean id="naverLoginBO"
		class="com.funding.sprout.user.controller.NaverLoginBO" />

	<!-- KaKaoLoginBO Class에 대한 Bean설정 추가 -->
	<beans:bean id="kakaoLoginBO"
		class="com.funding.sprout.user.controller.KakaoLoginBO" />

	<!-- Interceptor -->
	<beans:bean id="Interceptor"
		class="com.funding.sprout.user.controller.Interceptor" />
	<interceptors>
		<interceptor>
			<mapping path="/user/login*" />
			<beans:ref bean="Interceptor" />
		</interceptor>
	</interceptors>
	
	<websocket:handlers> 
		<websocket:mapping handler="EchoHandler" path="/handler"/>
	<websocket:handshake-interceptors> 
		<beans:bean class="org.springframework.web.socket.server.support.HttpSessionHandshakeInterceptor"/>
		</websocket:handshake-interceptors>
	</websocket:handlers>
	<beans:bean id="EchoHandler" class="com.funding.sprout.message.controller.EchoHandler"/>

	<beans:bean class="org.springframework.web.socket.server.standard.ServletServerContainerFactoryBean">
		<beans:property name="maxTextMessageBufferSize" value="8192" />
		<beans:property name="maxBinaryMessageBufferSize" value="8192" />
	</beans:bean>

	<context:component-scan
		base-package="com.funding.sprout" />

	<!-- 추가한것들은 여기 아래부터  -->
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />
	
	<!-- Spring MVC AOP 어노테이션 자동 등록 -->
	<aop:aspectj-autoproxy />
	
</beans:beans>
